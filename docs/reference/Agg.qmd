# Agg { #fasttrackpy.Agg }

`Agg(self, method='agg_sum', **kwargs)`

_summary_

## Parameters

| Name     | Type                   | Description                           | Default     |
|----------|------------------------|---------------------------------------|-------------|
| `method` | Union\[str, Callable\] | _description_. Defaults to "agg_sum". | `'agg_sum'` |